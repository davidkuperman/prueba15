<%= form_for(@ceo) do |f| %>
  <% if @ceo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ceo.errors.count, "error") %> prohibited this ceo from being saved:</h2>

      <ul>
      <% @ceo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
